{
  "name": "kscw-website",
  "type": "module",
  "version": "1.28.2",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "site-text": "node scripts/fetch-site-text.mjs",
    "predev": "npm run site-text",
    "dev": "astro dev",
    "prebuild": "npm run site-text",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:all": "vitest run && playwright test",
    "prepare": "git config core.hooksPath .githooks || true"
  },
  "dependencies": {
    "@phosphor-icons/core": "^2.1.1",
    "astro": "^6.0.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "vitest": "^4.1.5"
  }
}
