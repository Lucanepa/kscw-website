---
import PageLayout from '../../layouts/PageLayout.astro';
import { t } from '../../lib/i18n';
import { versionedAsset } from '../../lib/assetVersion';

const locale = 'de';

// All current Zürich cantonal schools (Mittelschulen) for the "other cantonal
// school" dropdown, alphabetical. Excludes KS Wiedikon on purpose — that's the
// dedicated KWI option above (which stores "KS Wiedikon"). Shown only when
// "Yes, other cantonal school" is selected. Notes: Rämibühl is three separate
// schools sharing the campus (Literar-/Real-/MN-Gymnasium); Oerlikon + Birch are
// the two campuses of KS Zürich Nord (2019 merger) — kept as students still use
// those names; Dübendorf opens Aug 2026 as a KS Stadelhofen branch.
const KS_OTHER = [
  'KS Birch', 'KS Büelrain', 'KS Bülach', 'KS Dübendorf', 'KS Enge',
  'KS Freudenberg', 'KS Hohe Promenade', 'KS Hottingen', 'KS Im Lee',
  'KS Küsnacht', 'KS Limmattal', 'KS Oerlikon',
  'KS Rämibühl (Literargymnasium)', 'KS Rämibühl (MN-Gymnasium)', 'KS Rämibühl (Realgymnasium)',
  'KS Riesbach', 'KS Rychenberg', 'KS Stadelhofen', 'KS Uetikon am See',
  'KS Uster', 'KS Wetzikon', 'KS Zimmerberg', 'KS Zürich Nord',
  'Liceo Artistico',
];
---
<PageLayout
  title="Anmeldung — KSC Wiedikon"
  titleKey="weiteresAnmeldungMetaTitle"
  description="Mitglied werden beim KSC Wiedikon — Anmeldung für Volleyball, Basketball und Passivmitgliedschaft."
  descriptionKey="weiteresAnmeldungMetaDescription"
  locale={locale}
  pageTitle={t(locale, 'registrationTitle')}
  pageTitleKey="registrationTitle"
  pageSubtitle={t(locale, 'registrationSubtitle')}
  pageSubtitleKey="registrationSubtitle"
>
  <section class="section">
    <div class="container" style="max-width: 720px;">

      <!-- Coach Note -->
      <div class="card" style="border-left: 4px solid var(--kscw-gold); margin-bottom: var(--space-xl);">
        <div class="card-body">
          <p style="color: var(--text-secondary); display: flex; align-items: flex-start; gap: var(--space-sm); margin: 0;">
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="var(--kscw-gold)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="flex-shrink: 0; margin-top: 2px;"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4"/><path d="M12 8h.01"/></svg>
            <span data-i18n="registrationCoachNote">{t(locale, 'registrationCoachNote')}</span>
          </p>
        </div>
      </div>

      <form id="registration-form">

        <!-- Membership Type -->
        <div class="form-group">
          <label><span data-i18n="registrationType">{t(locale, 'registrationType')}</span> *</label>
          <div style="display: flex; gap: var(--space-md); flex-wrap: wrap; margin-top: var(--space-xs);">
            <label class="radio-card">
              <input type="radio" name="membership_type" value="volleyball" required>
              <span data-i18n="registrationTypeVolleyball">{t(locale, 'registrationTypeVolleyball')}</span>
            </label>
            <label class="radio-card">
              <input type="radio" name="membership_type" value="basketball" required>
              <span data-i18n="registrationTypeBasketball">{t(locale, 'registrationTypeBasketball')}</span>
            </label>
            <label class="radio-card">
              <input type="radio" name="membership_type" value="passive" required>
              <span data-i18n="registrationTypePassive">{t(locale, 'registrationTypePassive')}</span>
            </label>
          </div>
        </div>

        <!-- Common Fields -->
        <div class="form-row">
          <div class="form-group">
            <label for="vorname"><span data-i18n="registrationFirstName">{t(locale, 'registrationFirstName')}</span> *</label>
            <input type="text" id="vorname" name="vorname" class="form-input" required>
            <small class="name-charset-warn" data-for="vorname" style="color: var(--kscw-gold, #b8860b); display: none;" data-i18n="registrationNameCharsetWarn">{t(locale, 'registrationNameCharsetWarn')}</small>
          </div>
          <div class="form-group">
            <label for="nachname"><span data-i18n="registrationLastName">{t(locale, 'registrationLastName')}</span> *</label>
            <input type="text" id="nachname" name="nachname" class="form-input" required>
            <small class="name-charset-warn" data-for="nachname" style="color: var(--kscw-gold, #b8860b); display: none;" data-i18n="registrationNameCharsetWarn">{t(locale, 'registrationNameCharsetWarn')}</small>
          </div>
        </div>

        <div class="form-group">
          <label for="email"><span data-i18n="registrationEmail">{t(locale, 'registrationEmail')}</span> *</label>
          <input type="email" id="email" name="email" class="form-input" required>
        </div>

        <!-- Already a member. Shown once name + last name + email together match
             an ACTIVE member (POST /kscw/registration/check-duplicate); the
             submit button is disabled while it is visible. Existing members
             re-registering was a recurring, silent source of duplicate member
             records — five of the club's first 36 registrations. Never fires on
             a shared family mailbox: the check needs the whole identity. -->
        <div id="already-member-warn" role="alert" style="display: none;">
          <strong data-i18n="registrationAlreadyMemberTitle">{t(locale, 'registrationAlreadyMemberTitle')}</strong>
          <!-- Split into plain data-i18n pieces around literal links: the client
               i18n engine applies translations as textContent ONLY and has no
               innerHTML path by design (public/js/i18n.js), so markup inside a
               dictionary value would render as visible tags after the English
               swap. Same idiom as the privacy-consent block in club/kontakt.astro. -->
          <span>
            <span data-i18n="registrationAlreadyMemberPre">{t(locale, 'registrationAlreadyMemberPre')}</span>
            <a href="https://wiedisync.kscw.ch" target="_blank" rel="noopener noreferrer">wiedisync.kscw.ch</a><span data-i18n="registrationAlreadyMemberMid">{t(locale, 'registrationAlreadyMemberMid')}</span>
            <a href="mailto:admin@wiedisync.kscw.ch">admin@wiedisync.kscw.ch</a><span data-i18n="registrationAlreadyMemberPost">{t(locale, 'registrationAlreadyMemberPost')}</span>
          </span>
        </div>

        <div class="form-group">
          <label for="telefon"><span data-i18n="registrationPhone">{t(locale, 'registrationPhone')}</span> *</label>
          <input type="tel" id="telefon" name="telefon" class="form-input" required>
        </div>

        <div class="form-group">
          <label for="adresse"><span data-i18n="registrationAddress">{t(locale, 'registrationAddress')}</span> *</label>
          <input type="text" id="adresse" name="adresse" class="form-input" required>
        </div>

        <div class="form-row">
          <div class="form-group" style="flex: 0 0 120px; min-width: 0;">
            <label for="plz"><span data-i18n="registrationPLZ">{t(locale, 'registrationPLZ')}</span> *</label>
            <input type="text" id="plz" name="plz" class="form-input" required>
          </div>
          <div class="form-group">
            <label for="ort"><span data-i18n="registrationCity">{t(locale, 'registrationCity')}</span> *</label>
            <input type="text" id="ort" name="ort" class="form-input" required>
          </div>
        </div>

        <div class="form-group">
          <label for="iban"><span data-i18n="registrationIBAN">{t(locale, 'registrationIBAN')}</span> *</label>
          <input type="text" id="iban" name="iban" class="form-input" placeholder="CH00 0000 0000 0000 0000 0" required>
          <small style="color: var(--text-secondary);" data-i18n="registrationIBANNote">{t(locale, 'registrationIBANNote')}</small>
        </div>

        <div class="form-row">
          <div class="form-group">
            <label for="geburtsdatum"><span data-i18n="registrationDOB">{t(locale, 'registrationDOB')}</span> *</label>
            <input type="date" id="geburtsdatum" name="geburtsdatum" class="form-input" required>
          </div>
          <div class="form-group">
            <label for="geschlecht"><span data-i18n="registrationGender">{t(locale, 'registrationGender')}</span> *</label>
            <select id="geschlecht" name="geschlecht" class="form-select" required>
              <option value="" disabled selected>—</option>
              <option value="männlich" data-i18n="registrationGenderMale">{t(locale, 'registrationGenderMale')}</option>
              <option value="weiblich" data-i18n="registrationGenderFemale">{t(locale, 'registrationGenderFemale')}</option>
            </select>
          </div>
        </div>

        <!-- Multi-select: dual nationals pick every passport they hold. The first
             one chosen is the primary and is what ClubDesk (single-valued) gets. -->
        <div class="form-group nationality-wrapper" id="nationality-wrapper">
          <label><span data-i18n="registrationNationality">{t(locale, 'registrationNationality')}</span> *</label>
          <small style="color: var(--text-secondary); display: block; margin-bottom: var(--space-xs);" data-i18n="registrationNationalityHint">{t(locale, 'registrationNationalityHint')}</small>
          <input type="hidden" id="nationalitaet" name="nationalitaet" required>
          <input type="hidden" id="nationalitaet_codes" name="nationalitaet_codes">
          <div class="country-chips" id="nationality-chips"></div>
          <button type="button" class="form-select nationality-trigger" id="nationality-trigger">
            <span id="nationality-trigger-text">—</span>
            <svg width="12" height="12" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="2 4 6 8 10 4"/></svg>
          </button>
          <div class="nationality-dropdown" id="nationality-dropdown">
            <input type="text" class="form-input nationality-search" id="nationality-search" placeholder={t(locale, 'weiteresAnmeldungSearchPlaceholder')} data-i18n-placeholder="weiteresAnmeldungSearchPlaceholder" autocomplete="off">
            <div class="nationality-options" id="nationality-options"></div>
          </div>
        </div>

        <!-- The national federation that FIRST licensed the player — their
             federation of origin, not the most recent one. There is no "none"
             option: a first-ever licence is issued by Swiss Volley / Swiss
             Basketball, so that answer is Switzerland.
             Mandatory for volleyball (except guests) — enforced in JS on submit;
             the star is toggled by registration-form.js to match. -->
        <div class="form-group nationality-wrapper" id="federation-wrapper">
          <label><span data-i18n="registrationFederation">{t(locale, 'registrationFederation')}</span><span id="federation-required-star" style="display: none;"> *</span></label>
          <small style="color: var(--text-secondary); display: block; margin-bottom: var(--space-xs);" data-i18n="registrationFederationHint">{t(locale, 'registrationFederationHint')}</small>
          <input type="hidden" id="federation_of_origin" name="federation_of_origin">
          <button type="button" class="form-select nationality-trigger" id="federation-trigger">
            <span id="federation-trigger-text">—</span>
            <svg width="12" height="12" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="2 4 6 8 10 4"/></svg>
          </button>
          <div class="nationality-dropdown" id="federation-dropdown">
            <input type="text" class="form-input nationality-search" id="federation-search" placeholder={t(locale, 'weiteresAnmeldungSearchPlaceholder')} data-i18n-placeholder="weiteresAnmeldungSearchPlaceholder" autocomplete="off">
            <div class="nationality-options" id="federation-options"></div>
          </div>
        </div>

        <!-- ═══ VOLLEYBALL-ONLY FIELDS ═══ -->
        <div id="vb-fields" style="display: none;">
          <input type="hidden" id="anrede" name="anrede">

          <div class="form-group">
            <label for="funktion-vb"><span data-i18n="registrationFunktion">{t(locale, 'registrationFunktion')}</span> *</label>
            <select id="funktion-vb" name="funktion_vb" class="form-select" data-conditional-required>
              <option value="" disabled selected data-i18n="registrationFunktionPlaceholder">{t(locale, 'registrationFunktionPlaceholder')}</option>
              <option value="Spieler*in" data-i18n="registrationFunktionPlayer">{t(locale, 'registrationFunktionPlayer')}</option>
              <option value="Guest" data-i18n="registrationFunktionGuest">{t(locale, 'registrationFunktionGuest')}</option>
              <option value="Trainer*in" data-i18n="registrationFunktionCoach">{t(locale, 'registrationFunktionCoach')}</option>
              <option value="Teamverantwortliche*r" data-i18n="registrationFunktionTR">{t(locale, 'registrationFunktionTR')}</option>
              <option value="Andere" data-i18n="registrationFunktionOther">{t(locale, 'registrationFunktionOther')}</option>
            </select>
          </div>

          <div class="form-group team-wrapper" id="vb-team-wrapper" style="display: none;">
            <label><span data-i18n="registrationTeam">{t(locale, 'registrationTeam')}</span> *</label>
            <button type="button" class="form-select team-trigger" id="vb-team-trigger">
              <span id="vb-team-trigger-text" data-i18n="registrationTeamPlaceholder">{t(locale, 'registrationTeamPlaceholder')}</span>
              <svg width="12" height="12" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="2 4 6 8 10 4"/></svg>
            </button>
            <div class="team-dropdown" id="vb-team-dropdown">
              <div class="team-options" id="vb-team"></div>
            </div>
          </div>

          <div class="form-group">
            <label for="vb-fee"><span data-i18n="registrationFeeCategory">{t(locale, 'registrationFeeCategory')}</span> *</label>
            <select id="vb-fee" name="vb_fee" class="form-select" data-conditional-required>
              <option value="" disabled selected data-i18n="registrationFeeCategoryPlaceholder">{t(locale, 'registrationFeeCategoryPlaceholder')}</option>
              <option value="VB Turnier KWI" data-i18n="registrationVBFeeTournamentKWI">{t(locale, 'registrationVBFeeTournamentKWI')}</option>
              <option value="VB Schüler*in Turnier" data-i18n="registrationVBFeeTournament">{t(locale, 'registrationVBFeeTournament')}</option>
              <option value="VB Schüler*in Meisterschaft" data-i18n="registrationVBFeeHighSchool">{t(locale, 'registrationVBFeeHighSchool')}</option>
              <option value="VB Student*in Meisterschaft" data-i18n="registrationVBFeeStudent">{t(locale, 'registrationVBFeeStudent')}</option>
              <option value="VB Erwerbstätige" data-i18n="registrationVBFeeWorking">{t(locale, 'registrationVBFeeWorking')}</option>
              <!-- Coach only: registration-form.js reveals it for "Trainer*in" and hides
                   the rest (the engine bills a coach of an active team as Gratis). -->
              <option value="Gratis" data-i18n="registrationFeeGratisCoach" hidden disabled>{t(locale, 'registrationFeeGratisCoach')}</option>
            </select>
          </div>

          <div class="form-group" id="vb-ahv-group">
            <label for="vb-ahv"><span data-i18n="registrationAHV">{t(locale, 'registrationAHV')}</span> <span id="vb-ahv-star">*</span></label>
            <input type="text" id="vb-ahv" name="vb_ahv" class="form-input">
            <small style="color: var(--text-secondary);" data-i18n="registrationAHVNote">{t(locale, 'registrationAHVNote')}</small>
          </div>

          <div class="form-group js-guest-hide">
            <label for="kantonsschule-vb"><span data-i18n="registrationKantonsschule">{t(locale, 'registrationKantonsschule')}</span> *</label>
            <select id="kantonsschule-vb" name="kantonsschule_vb" class="form-select" data-conditional-required>
              <option value="" disabled selected>—</option>
              <option value="Nein" data-i18n="registrationKantonsschuleNo">{t(locale, 'registrationKantonsschuleNo')}</option>
              <option value="KS Wiedikon" data-i18n="registrationKantonsschuleWiedikon">{t(locale, 'registrationKantonsschuleWiedikon')}</option>
              <option value="Andere Kantonsschule" data-i18n="registrationKantonsschuleOther">{t(locale, 'registrationKantonsschuleOther')}</option>
            </select>
          </div>

          <div class="form-group" id="ks-other-vb-group" style="display:none;">
            <label for="ks-other-vb"><span data-i18n="registrationKantonsschuleWhich">{t(locale, 'registrationKantonsschuleWhich')}</span> *</label>
            <select id="ks-other-vb" name="kantonsschule_other_vb" class="form-select">
              <option value="" disabled selected>—</option>
              {KS_OTHER.map((s) => <option value={s}>{s}</option>)}
            </select>
          </div>

          <div class="form-group js-guest-hide">
            <label data-i18n="registrationLicence">{t(locale, 'registrationLicence')}</label>
            <div class="toggle-group">
              <label class="toggle-label">
                <span data-i18n="registrationLicenceSchreiber">{t(locale, 'registrationLicenceSchreiber')}</span>
                <span class="toggle-switch"><input type="checkbox" name="lizenz_vb" value="Schreiber"><span class="toggle-slider"></span></span>
              </label>
              <label class="toggle-label">
                <span data-i18n="registrationLicenceSchiedsrichter">{t(locale, 'registrationLicenceSchiedsrichter')}</span>
                <span class="toggle-switch"><input type="checkbox" name="lizenz_vb" value="Schiedsrichter" id="vb-ref-check"><span class="toggle-slider"></span></span>
              </label>
            </div>
          </div>
          <div class="form-group" id="vb-ref-level-group" style="display: none;">
            <label for="vb-ref-level"><span data-i18n="registrationLicenceSchiedsrichter">{t(locale, 'registrationLicenceSchiedsrichter')}</span> — <span data-i18n="weiteresAnmeldungLevel">{t(locale, 'weiteresAnmeldungLevel')}</span></label>
            <select id="vb-ref-level" name="vb_ref_level" class="form-select">
              <option value="" disabled selected data-i18n="registrationLicenceRefLevelPlaceholder">{t(locale, 'registrationLicenceRefLevelPlaceholder')}</option>
              <option value="N4-3">N4-3</option>
              <option value="N4-2">N4-2</option>
              <option value="N4-1">N4-1</option>
              <option value="N3-3">N3-3</option>
              <option value="N3-2">N3-2</option>
              <option value="N3-1">N3-1</option>
              <option value="N2-2">N2-2</option>
              <option value="N2-1">N2-1</option>
              <option value="N1 (NL-Kader)">N1 (NL-Kader)</option>
            </select>
          </div>
        </div>

        <!-- ═══ BASKETBALL-ONLY FIELDS ═══ -->
        <div id="bb-fields" style="display: none;">
          <div class="form-group">
            <label for="funktion-bb"><span data-i18n="registrationFunktion">{t(locale, 'registrationFunktion')}</span> *</label>
            <select id="funktion-bb" name="funktion_bb" class="form-select" data-conditional-required>
              <option value="" disabled selected data-i18n="registrationFunktionPlaceholder">{t(locale, 'registrationFunktionPlaceholder')}</option>
              <option value="Spieler*in" data-i18n="registrationFunktionPlayer">{t(locale, 'registrationFunktionPlayer')}</option>
              <option value="Guest" data-i18n="registrationFunktionGuest">{t(locale, 'registrationFunktionGuest')}</option>
              <option value="Trainer*in" data-i18n="registrationFunktionCoach">{t(locale, 'registrationFunktionCoach')}</option>
              <option value="Teamverantwortliche*r" data-i18n="registrationFunktionTR">{t(locale, 'registrationFunktionTR')}</option>
              <option value="Andere" data-i18n="registrationFunktionOther">{t(locale, 'registrationFunktionOther')}</option>
            </select>
          </div>

          <div class="form-group team-wrapper" id="bb-team-wrapper" style="display: none;">
            <label><span data-i18n="registrationTeam">{t(locale, 'registrationTeam')}</span> *</label>
            <button type="button" class="form-select team-trigger" id="bb-team-trigger">
              <span id="bb-team-trigger-text" data-i18n="registrationTeamPlaceholder">{t(locale, 'registrationTeamPlaceholder')}</span>
              <svg width="12" height="12" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="2 4 6 8 10 4"/></svg>
            </button>
            <div class="team-dropdown" id="bb-team-dropdown">
              <div class="team-options" id="bb-team"></div>
            </div>
          </div>

          <div class="form-group">
            <label for="bb-fee"><span data-i18n="registrationFeeCategory">{t(locale, 'registrationFeeCategory')}</span> *</label>
            <select id="bb-fee" name="bb_fee" class="form-select" data-conditional-required>
              <option value="" disabled selected data-i18n="registrationFeeCategoryPlaceholder">{t(locale, 'registrationFeeCategoryPlaceholder')}</option>
              <option value="BB Erwerbstätige 1. Liga" data-i18n="registrationBBFeeWorking1L">{t(locale, 'registrationBBFeeWorking1L')}</option>
              <option value="BB Lernende/Studierende 1. Liga" data-i18n="registrationBBFeeStudent1L">{t(locale, 'registrationBBFeeStudent1L')}</option>
              <option value="BB Erwerbstätige" data-i18n="registrationBBFeeWorking">{t(locale, 'registrationBBFeeWorking')}</option>
              <option value="BB Lernende/Studierende" data-i18n="registrationBBFeeStudent">{t(locale, 'registrationBBFeeStudent')}</option>
              <option value="BB Jugend Meisterschaft" data-i18n="registrationBBFeeJunior">{t(locale, 'registrationBBFeeJunior')}</option>
              <option value="BB Minis Turnier" data-i18n="registrationBBFeeMinis">{t(locale, 'registrationBBFeeMinis')}</option>
              <!-- Coach only — see the volleyball select. -->
              <option value="Gratis" data-i18n="registrationFeeGratisCoach" hidden disabled>{t(locale, 'registrationFeeGratisCoach')}</option>
            </select>
          </div>

          <div class="form-group" id="bb-ahv-group">
            <label for="bb-ahv"><span data-i18n="registrationAHV">{t(locale, 'registrationAHV')}</span> <span id="bb-ahv-star">*</span></label>
            <input type="text" id="bb-ahv" name="bb_ahv" class="form-input">
            <small style="color: var(--text-secondary);" data-i18n="registrationAHVNote">{t(locale, 'registrationAHVNote')}</small>
          </div>

          <div class="form-group js-guest-hide">
            <label for="kantonsschule-bb"><span data-i18n="registrationKantonsschule">{t(locale, 'registrationKantonsschule')}</span> *</label>
            <select id="kantonsschule-bb" name="kantonsschule_bb" class="form-select" data-conditional-required>
              <option value="" disabled selected>—</option>
              <option value="Nein" data-i18n="registrationKantonsschuleNo">{t(locale, 'registrationKantonsschuleNo')}</option>
              <option value="KS Wiedikon" data-i18n="registrationKantonsschuleWiedikon">{t(locale, 'registrationKantonsschuleWiedikon')}</option>
              <option value="Andere Kantonsschule" data-i18n="registrationKantonsschuleOther">{t(locale, 'registrationKantonsschuleOther')}</option>
            </select>
          </div>

          <div class="form-group" id="ks-other-bb-group" style="display:none;">
            <label for="ks-other-bb"><span data-i18n="registrationKantonsschuleWhich">{t(locale, 'registrationKantonsschuleWhich')}</span> *</label>
            <select id="ks-other-bb" name="kantonsschule_other_bb" class="form-select">
              <option value="" disabled selected>—</option>
              {KS_OTHER.map((s) => <option value={s}>{s}</option>)}
            </select>
          </div>

          <div class="form-group js-guest-hide">
            <label data-i18n="registrationLicenceBBScorer">{t(locale, 'registrationLicenceBBScorer')}</label>
            <div style="display: flex; gap: var(--space-lg); flex-wrap: wrap; margin-top: var(--space-xs);">
              <label class="radio-card">
                <input type="radio" name="bb_scorer_licence" value="">
                <span data-i18n="registrationLicenceNone">{t(locale, 'registrationLicenceNone')}</span>
              </label>
              <label class="radio-card">
                <input type="radio" name="bb_scorer_licence" value="OTR 1">
                <span data-i18n="registrationLicenceOTR1">{t(locale, 'registrationLicenceOTR1')}</span>
              </label>
              <label class="radio-card">
                <input type="radio" name="bb_scorer_licence" value="OTR 2">
                <span data-i18n="registrationLicenceOTR2">{t(locale, 'registrationLicenceOTR2')}</span>
              </label>
              <label class="radio-card">
                <input type="radio" name="bb_scorer_licence" value="OTN">
                <span data-i18n="registrationLicenceOTN">{t(locale, 'registrationLicenceOTN')}</span>
              </label>
            </div>
          </div>

          <div class="form-group js-guest-hide">
            <label data-i18n="registrationLicenceBBReferee">{t(locale, 'registrationLicenceBBReferee')}</label>
            <div class="toggle-group">
              <label class="toggle-label">
                <span data-i18n="registrationLicenceSchiedsrichter">{t(locale, 'registrationLicenceSchiedsrichter')}</span>
                <span class="toggle-switch"><input type="checkbox" name="bb_referee" value="Schiedsrichter" id="bb-ref-check"><span class="toggle-slider"></span></span>
              </label>
            </div>
          </div>

          <div class="form-group js-guest-hide">
            <label><span data-i18n="registrationIDUpload">{t(locale, 'registrationIDUpload')}</span> *</label>
            <small style="color: var(--text-secondary); display: block; margin-bottom: var(--space-sm);" data-i18n="registrationIDUploadNote">{t(locale, 'registrationIDUploadNote')}</small>
            <div style="display: flex; gap: var(--space-md); flex-wrap: wrap;">
              <div>
                <label for="id-front" style="font-size: var(--text-sm); font-weight: 600; display: block; margin-bottom: var(--space-xs);"><span data-i18n="registrationIDFront">{t(locale, 'registrationIDFront')}</span> *</label>
                <input type="file" id="id-front" name="id_front" accept="image/*,.pdf" data-conditional-required>
              </div>
              <div>
                <label for="id-back" style="font-size: var(--text-sm); font-weight: 600; display: block; margin-bottom: var(--space-xs);"><span data-i18n="registrationIDBack">{t(locale, 'registrationIDBack')}</span> *</label>
                <input type="file" id="id-back" name="id_back" accept="image/*,.pdf">
              </div>
            </div>
          </div>

          <div class="form-group js-guest-hide" id="bb-situation-group">
            <label><span data-i18n="registrationBBSituation">{t(locale, 'registrationBBSituation')}</span> *</label>
            <small style="color: var(--text-secondary); display: block; margin-bottom: var(--space-sm);" data-i18n="registrationBBSituationHint">{t(locale, 'registrationBBSituationHint')}</small>
            <div style="display: flex; flex-direction: column; gap: var(--space-sm); margin-top: var(--space-xs);">
              <label class="radio-card">
                <input type="radio" name="bb_situation" value="neu">
                <span data-i18n="registrationBBSituationNew">{t(locale, 'registrationBBSituationNew')}</span>
              </label>
              <label class="radio-card">
                <input type="radio" name="bb_situation" value="transfer_ch">
                <span data-i18n="registrationBBSituationTransferCH">{t(locale, 'registrationBBSituationTransferCH')}</span>
              </label>
              <label class="radio-card">
                <input type="radio" name="bb_situation" value="transfer_intl">
                <span data-i18n="registrationBBSituationTransferIntl">{t(locale, 'registrationBBSituationTransferIntl')}</span>
              </label>
              <label class="radio-card">
                <input type="radio" name="bb_situation" value="rueckkehr">
                <span data-i18n="registrationBBSituationReturner">{t(locale, 'registrationBBSituationReturner')}</span>
              </label>
            </div>
          </div>

          <!-- Only shown for a transfer from another Swiss club: Swiss Basketball
               waives the Freibrief when the player held no licence in the last two
               seasons, so asking here spares them chasing a club that owes nothing. -->
          <div class="form-group js-guest-hide" id="bb-recent-licence-group" style="display: none;">
            <label><span data-i18n="registrationBBRecentLicence">{t(locale, 'registrationBBRecentLicence')}</span> *</label>
            <small style="color: var(--text-secondary); display: block; margin-bottom: var(--space-sm);" data-i18n="registrationBBRecentLicenceHint">{t(locale, 'registrationBBRecentLicenceHint')}</small>
            <div style="display: flex; flex-direction: column; gap: var(--space-sm); margin-top: var(--space-xs);">
              <label class="radio-card">
                <input type="radio" name="bb_recent_licence" value="ja">
                <span data-i18n="registrationBBRecentLicenceYes">{t(locale, 'registrationBBRecentLicenceYes')}</span>
              </label>
              <label class="radio-card">
                <input type="radio" name="bb_recent_licence" value="nein">
                <span data-i18n="registrationBBRecentLicenceNo">{t(locale, 'registrationBBRecentLicenceNo')}</span>
              </label>
            </div>
          </div>

          <div class="form-group js-guest-hide" id="bb-docs-group">
            <label data-i18n="registrationBBDocs">{t(locale, 'registrationBBDocs')}</label>
            <small style="color: var(--text-secondary); display: block; margin-bottom: var(--space-sm);" data-i18n="registrationBBDocsHint">{t(locale, 'registrationBBDocsHint')}</small>
            <div class="bb-docs-list">
              <div class="bb-doc-item" data-doc="lizenz">
                <span data-i18n="registrationBBDocsLizenz">{t(locale, 'registrationBBDocsLizenz')}</span>
                <a href="/docs/lizenzantrag-swiss-basketball.pdf" target="_blank" rel="noopener noreferrer" class="bb-doc-download" id="bb-doc-lizenz" data-i18n="registrationBBDocsLizenzDownload">{t(locale, 'registrationBBDocsLizenzDownload')}</a>
              </div>
              <div class="bb-doc-item bb-doc-cond" data-doc="freibrief" style="display: none;">
                <span data-i18n="registrationBBDocsFreibrief">{t(locale, 'registrationBBDocsFreibrief')}</span>
                <a href="/docs/freibrief-swiss-basketball.pdf" target="_blank" rel="noopener noreferrer" class="bb-doc-download" id="bb-doc-freibrief" data-i18n="registrationBBDocsFreibriefDownload">{t(locale, 'registrationBBDocsFreibriefDownload')}</a>
              </div>
              <div class="bb-doc-item bb-doc-cond" data-doc="selfdecl" style="display: none;">
                <span data-i18n="registrationBBDocsSelfDecl">{t(locale, 'registrationBBDocsSelfDecl')}</span>
                <a href="/docs/player-self-declaration-fiba.pdf" target="_blank" rel="noopener noreferrer" class="bb-doc-download" id="bb-doc-selfdecl" data-i18n="registrationBBDocsSelfDeclDownload">{t(locale, 'registrationBBDocsSelfDeclDownload')}</a>
              </div>
              <div class="bb-doc-item bb-doc-cond" data-doc="natdecl" style="display: none;">
                <span data-i18n="registrationBBDocsNatDecl">{t(locale, 'registrationBBDocsNatDecl')}</span>
                <a href="/docs/acknowledgment-national-team-restriction-fiba.pdf" target="_blank" rel="noopener noreferrer" class="bb-doc-download" id="bb-doc-natdecl" data-i18n="registrationBBDocsNatDeclDownload">{t(locale, 'registrationBBDocsNatDeclDownload')}</a>
              </div>
              <div class="bb-doc-item bb-doc-cond" data-doc="u18parents" style="display: none;">
                <span data-i18n="registrationBBDocsU18Parents">{t(locale, 'registrationBBDocsU18Parents')}</span>
                <a href="/docs/u18-parents-authorisation-fiba.pdf" target="_blank" rel="noopener noreferrer" class="bb-doc-download" id="bb-doc-u18parents" data-i18n="registrationBBDocsU18ParentsDownload">{t(locale, 'registrationBBDocsU18ParentsDownload')}</a>
              </div>
            </div>
          </div>

          <div class="form-group js-guest-hide" id="bb-docs-upload-group">
            <label data-i18n="registrationBBDocsUpload">{t(locale, 'registrationBBDocsUpload')}</label>
            <small style="color: var(--text-secondary); display: block; margin-bottom: var(--space-sm);" data-i18n="registrationBBDocsUploadNote">{t(locale, 'registrationBBDocsUploadNote')}</small>
            <div style="display: flex; flex-direction: column; gap: var(--space-md);">
              <div data-doc-upload="lizenz">
                <label for="bb-doc-lizenz-upload" style="font-size: var(--text-sm); font-weight: 600; display: block; margin-bottom: var(--space-xs);"><span data-i18n="registrationBBDocsUploadLizenz">{t(locale, 'registrationBBDocsUploadLizenz')}</span> *</label>
                <input type="file" id="bb-doc-lizenz-upload" name="bb_doc_lizenz" accept=".pdf,.jpg,.jpeg,.png,.webp" data-conditional-required>
              </div>
              <div class="bb-doc-cond" data-doc-upload="freibrief" style="display: none;">
                <label for="bb-doc-freibrief-upload" style="font-size: var(--text-sm); font-weight: 600; display: block; margin-bottom: var(--space-xs);"><span data-i18n="registrationBBDocsUploadFreibrief">{t(locale, 'registrationBBDocsUploadFreibrief')}</span> *</label>
                <input type="file" id="bb-doc-freibrief-upload" name="bb_doc_freibrief" accept=".pdf,.jpg,.jpeg,.png,.webp">
              </div>
              <div class="bb-doc-cond" data-doc-upload="selfdecl" style="display: none;">
                <label for="bb-doc-selfdecl-upload" style="font-size: var(--text-sm); font-weight: 600; display: block; margin-bottom: var(--space-xs);"><span data-i18n="registrationBBDocsUploadSelfDecl">{t(locale, 'registrationBBDocsUploadSelfDecl')}</span> *</label>
                <input type="file" id="bb-doc-selfdecl-upload" name="bb_doc_selfdecl" accept=".pdf,.jpg,.jpeg,.png,.webp">
              </div>
              <div class="bb-doc-cond" data-doc-upload="natdecl" style="display: none;">
                <label for="bb-doc-natdecl-upload" style="font-size: var(--text-sm); font-weight: 600; display: block; margin-bottom: var(--space-xs);"><span data-i18n="registrationBBDocsUploadNatDecl">{t(locale, 'registrationBBDocsUploadNatDecl')}</span> *</label>
                <input type="file" id="bb-doc-natdecl-upload" name="bb_doc_natdecl" accept=".pdf,.jpg,.jpeg,.png,.webp">
              </div>
              <div class="bb-doc-cond" data-doc-upload="u18parents" style="display: none;">
                <label for="bb-doc-u18parents-upload" style="font-size: var(--text-sm); font-weight: 600; display: block; margin-bottom: var(--space-xs);"><span data-i18n="registrationBBDocsUploadU18Parents">{t(locale, 'registrationBBDocsUploadU18Parents')}</span> *</label>
                <input type="file" id="bb-doc-u18parents-upload" name="bb_doc_u18parents" accept=".pdf,.jpg,.jpeg,.png,.webp">
              </div>
              <div class="bb-doc-cond" data-doc-upload="schoolcert" style="display: none;">
                <label for="bb-doc-schoolcert-upload" style="font-size: var(--text-sm); font-weight: 600; display: block; margin-bottom: var(--space-xs);"><span data-i18n="registrationBBDocsUploadSchoolCert">{t(locale, 'registrationBBDocsUploadSchoolCert')}</span></label>
                <small style="color: var(--text-secondary); display: block; margin-bottom: var(--space-xs);" data-i18n="registrationBBDocsSchoolCertNote">{t(locale, 'registrationBBDocsSchoolCertNote')}</small>
                <input type="file" id="bb-doc-schoolcert-upload" name="bb_doc_schoolcert" accept=".pdf,.jpg,.jpeg,.png,.webp">
              </div>
            </div>
          </div>
        </div>

        <!-- ═══ PASSIVE-ONLY FIELDS ═══ -->
        <div id="passive-fields" style="display: none;">
          <div class="form-group">
            <label for="passive-fee"><span data-i18n="registrationFeeCategory">{t(locale, 'registrationFeeCategory')}</span> *</label>
            <select id="passive-fee" name="passive_fee" class="form-select" data-conditional-required>
              <option value="" disabled selected data-i18n="registrationFeeCategoryPlaceholder">{t(locale, 'registrationFeeCategoryPlaceholder')}</option>
              <option value="Passivmitglied" data-i18n="registrationPassiveFeeStandard">{t(locale, 'registrationPassiveFeeStandard')}</option>
              <option value="Gratis" data-i18n="registrationPassiveFeeGratis">{t(locale, 'registrationPassiveFeeGratis')}</option>
            </select>
          </div>
          <div class="form-group">
            <label data-i18n="registrationPassiveLicenceVB">{t(locale, 'registrationPassiveLicenceVB')}</label>
            <div class="toggle-group">
              <label class="toggle-label">
                <span data-i18n="registrationLicenceSchreiber">{t(locale, 'registrationLicenceSchreiber')}</span>
                <span class="toggle-switch"><input type="checkbox" name="lizenz_passive" value="VB Schreiber"><span class="toggle-slider"></span></span>
              </label>
              <label class="toggle-label">
                <span data-i18n="registrationLicenceSchiedsrichter">{t(locale, 'registrationLicenceSchiedsrichter')}</span>
                <span class="toggle-switch"><input type="checkbox" name="lizenz_passive" value="VB Schiedsrichter" id="passive-vb-ref-check"><span class="toggle-slider"></span></span>
              </label>
            </div>
          </div>
          <div class="form-group" id="passive-vb-ref-level-group" style="display: none;">
            <label for="passive-vb-ref-level"><span data-i18n="registrationLicenceSchiedsrichter">{t(locale, 'registrationLicenceSchiedsrichter')}</span> — <span data-i18n="weiteresAnmeldungLevel">{t(locale, 'weiteresAnmeldungLevel')}</span></label>
            <select id="passive-vb-ref-level" name="passive_vb_ref_level" class="form-select">
              <option value="" disabled selected data-i18n="registrationLicenceRefLevelPlaceholder">{t(locale, 'registrationLicenceRefLevelPlaceholder')}</option>
              <option value="N4-3">N4-3</option>
              <option value="N4-2">N4-2</option>
              <option value="N4-1">N4-1</option>
              <option value="N3-3">N3-3</option>
              <option value="N3-2">N3-2</option>
              <option value="N3-1">N3-1</option>
              <option value="N2-2">N2-2</option>
              <option value="N2-1">N2-1</option>
              <option value="N1 (NL-Kader)">N1 (NL-Kader)</option>
            </select>
          </div>

          <div class="form-group">
            <label data-i18n="registrationPassiveLicenceBB">{t(locale, 'registrationPassiveLicenceBB')}</label>
            <div style="display: flex; gap: var(--space-md); flex-wrap: wrap; margin-top: var(--space-xs);">
              <label class="radio-card">
                <input type="radio" name="passive_bb_scorer" value="">
                <span data-i18n="registrationLicenceNone">{t(locale, 'registrationLicenceNone')}</span>
              </label>
              <label class="radio-card">
                <input type="radio" name="passive_bb_scorer" value="BB OTR 1">
                <span data-i18n="registrationLicenceOTR1">{t(locale, 'registrationLicenceOTR1')}</span>
              </label>
              <label class="radio-card">
                <input type="radio" name="passive_bb_scorer" value="BB OTR 2">
                <span data-i18n="registrationLicenceOTR2">{t(locale, 'registrationLicenceOTR2')}</span>
              </label>
              <label class="radio-card">
                <input type="radio" name="passive_bb_scorer" value="BB OTN">
                <span data-i18n="registrationLicenceOTN">{t(locale, 'registrationLicenceOTN')}</span>
              </label>
            </div>
            <div class="toggle-group" style="margin-top: var(--space-sm);">
              <label class="toggle-label">
                <span data-i18n="registrationLicenceSchiedsrichter">{t(locale, 'registrationLicenceSchiedsrichter')}</span>
                <span class="toggle-switch"><input type="checkbox" name="lizenz_passive" value="BB Schiedsrichter"><span class="toggle-slider"></span></span>
              </label>
            </div>
          </div>
        </div>

        <!-- Bemerkungen (all types) -->
        <div class="form-group">
          <label for="bemerkungen" data-i18n="registrationComments">{t(locale, 'registrationComments')}</label>
          <textarea id="bemerkungen" name="bemerkungen" class="form-textarea" placeholder={t(locale, 'registrationCommentsPlaceholder')} data-i18n-placeholder="registrationCommentsPlaceholder"></textarea>
        </div>

        <!-- Privacy Notice (collapsible) -->
        <details style="margin-bottom: var(--space-lg);">
          <summary style="cursor: pointer; font-weight: 600; color: var(--accent-text);" data-i18n="registrationPrivacyTitle">{t(locale, 'registrationPrivacyTitle')}</summary>
          <p style="margin-top: var(--space-sm); color: var(--text-secondary); font-size: var(--text-sm); line-height: 1.7; text-align: justify;" data-i18n="registrationPrivacyText">{t(locale, 'registrationPrivacyText')}</p>
        </details>

        <!-- Consent Checkbox -->
        <div class="form-group">
          <label style="display: flex; align-items: flex-start; gap: var(--space-sm); cursor: pointer; font-weight: normal;">
            <input type="checkbox" id="consent" name="consent" required style="margin-top: 4px; flex-shrink: 0;">
            <span style="font-size: var(--text-sm);" data-i18n="registrationConsent">{t(locale, 'registrationConsent')}</span>
          </label>
        </div>

        <!-- Fee summary. The category labels carry a price, but not the bill: the
             engine (wiedisync clubdesk-update.js feeBreakdown) adds CHF 100 to a
             member with scorer duty and no licence, takes CHF 110 off a guest and
             bills a coach as Gratis — none of which the form showed. An applicant
             budgeting "VB Erwerbstätige (CHF 440)" could be invoiced 540. Filled by
             registration-form.js from public/js/registration-fees.js, a mirror of
             that engine; hidden until a category is chosen. -->
        <div id="fee-summary" class="fee-summary" style="display: none;" aria-live="polite">
          <div id="fee-summary-title" class="fee-summary-title" data-i18n="registrationFeeSummaryTitle">{t(locale, 'registrationFeeSummaryTitle')}</div>
          <!-- Named by the visible title, so a screen reader announces the table
               as the season fee rather than "table, 2 columns, 5 rows". -->
          <table class="fee-table" aria-labelledby="fee-summary-title">
            <tbody>
              <tr id="fee-row-membership">
                <th scope="row" data-i18n="registrationFeeRowMembership">{t(locale, 'registrationFeeRowMembership')}</th>
                <td id="fee-amount-membership"></td>
              </tr>
              <!-- Each sport has its own wording for the same two positions: the
                   federation differs, and basketball's CHF 100 is the Offiziellen-
                   Pflicht (waived by an OTR/OTN table-official licence), not a
                   Schreiber licence. Both variants ship in the markup and
                   registration-form.js shows the one for the chosen sport, so the
                   language toggle owns every label as usual. -->
              <tr id="fee-row-licence" style="display: none;">
                <th scope="row">
                  <span class="fee-vb" data-i18n="registrationFeeRowLicence">{t(locale, 'registrationFeeRowLicence')}</span>
                  <span class="fee-bb" style="display: none;" data-i18n="registrationFeeRowLicenceBB">{t(locale, 'registrationFeeRowLicenceBB')}</span>
                </th>
                <td id="fee-amount-licence"></td>
              </tr>
              <tr id="fee-row-surcharge" style="display: none;">
                <th scope="row">
                  <span class="fee-vb" data-i18n="registrationFeeRowSurcharge">{t(locale, 'registrationFeeRowSurcharge')}</span>
                  <span class="fee-bb" style="display: none;" data-i18n="registrationFeeRowSurchargeBB">{t(locale, 'registrationFeeRowSurchargeBB')}</span>
                  <br>
                  <small class="fee-vb" data-i18n="registrationFeeSurchargeHint">{t(locale, 'registrationFeeSurchargeHint')}</small>
                  <small class="fee-bb" style="display: none;" data-i18n="registrationFeeSurchargeHintBB">{t(locale, 'registrationFeeSurchargeHintBB')}</small>
                </th>
                <td id="fee-amount-surcharge"></td>
              </tr>
              <tr id="fee-row-guest" style="display: none;">
                <th scope="row" data-i18n="registrationFeeRowGuest">{t(locale, 'registrationFeeRowGuest')}</th>
                <td id="fee-amount-guest"></td>
              </tr>
            </tbody>
            <tfoot>
              <tr id="fee-row-total">
                <th scope="row" data-i18n="registrationFeeRowTotal">{t(locale, 'registrationFeeRowTotal')}</th>
                <td id="fee-amount-total"></td>
              </tr>
            </tfoot>
          </table>
          <p id="fee-note-coach" style="display: none;" data-i18n="registrationFeeCoachNote">{t(locale, 'registrationFeeCoachNote')}</p>
          <p id="fee-note-referee" style="display: none;" data-i18n="registrationFeeRefereeNote">{t(locale, 'registrationFeeRefereeNote')}</p>
          <p class="fee-summary-note" data-i18n="registrationFeeSummaryNote">{t(locale, 'registrationFeeSummaryNote')}</p>
        </div>

        <!-- Turnstile -->
        <div id="turnstile-container" class="form-group"></div>

        <!-- Submit -->
        <button type="submit" class="form-submit" style="width: 100%; font-size: 1rem; padding: var(--space-md) var(--space-xl); justify-content: center;" data-i18n="registrationSubmit">
          {t(locale, 'registrationSubmit')}
        </button>
      </form>

      <div id="form-feedback" role="status" aria-live="polite" style="display: none;"></div>
    </div>
  </section>

  <script is:inline src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer></script>
  <!-- Cache-buster is the file's content hash (src/lib/assetVersion.ts) — a
       hand-written version was forgotten once and shipped a validation rule
       that cached browsers never ran. -->
  <!-- Must precede registration-form.js: it reads window.KSCW_FEDERATIONS at
       start-up to label the federation-of-origin picker. -->
  <script is:inline src={versionedAsset('js/federations.js')}></script>
  <!-- Must precede registration-form.js too: it reads window.KSCW_FEES when
       filling the fee summary and filtering the category select. -->
  <script is:inline src={versionedAsset('js/registration-fees.js')}></script>
  <script is:inline src={versionedAsset('js/registration-form.js')}></script>
  <script src="../../islands/scroll-animations.ts"></script>
</PageLayout>

<style>
  .form-group {
    margin-bottom: var(--space-md);
  }
  .form-group label {
    display: block;
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: var(--space-xs);
  }
  .form-input,
  .form-select,
  .form-textarea {
    width: 100%;
    padding: 0.625rem 0.875rem;
    font-size: var(--text-sm);
    font-family: 'Inter', sans-serif;
    border: 1px solid var(--border);
    border-radius: var(--radius-md, 8px);
    background: var(--bg-white);
    color: var(--text-primary);
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
    box-sizing: border-box;
  }
  .form-input:focus,
  .form-select:focus,
  .form-textarea:focus {
    outline: none;
    border-color: var(--kscw-blue, #4A55A2);
    box-shadow: 0 0 0 3px rgba(74, 85, 162, 0.15);
  }
  .form-textarea {
    min-height: 120px;
    resize: vertical;
  }
  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-md);
  }
  @media (max-width: 480px) {
    .form-row { grid-template-columns: 1fr; }
  }
  .form-submit {
    display: inline-flex;
    align-items: center;
    gap: var(--space-xs);
    padding: 0.75rem 1.5rem;
    font-size: var(--text-sm);
    font-weight: 600;
    font-family: 'Inter', sans-serif;
    color: #ffffff;
    background: var(--kscw-blue, #4A55A2);
    border: none;
    border-radius: var(--radius-md, 8px);
    cursor: pointer;
    transition: background 0.2s ease, transform 0.1s ease;
  }
  .form-submit:hover {
    background: #3b4590;
  }
  .form-submit:active {
    transform: scale(0.98);
  }
  .form-submit:disabled {
    opacity: 0.6;
    cursor: not-allowed;
  }
  /* Radio card pills */
  .radio-card {
    display: inline-flex;
    align-items: center;
    gap: var(--space-xs);
    padding: 0.5rem 1rem;
    border: 2px solid var(--border);
    border-radius: var(--radius-md, 8px);
    cursor: pointer;
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--text-secondary);
    background: var(--bg-white);
    transition: border-color 0.2s ease, color 0.2s ease, background 0.2s ease;
    user-select: none;
  }
  .radio-card input[type="radio"] {
    display: none;
  }
  .radio-card:has(input:checked) {
    border-color: var(--kscw-blue, #4A55A2);
    color: var(--accent-text, #4A55A2);
    background: rgba(74, 85, 162, 0.06);
  }
  .radio-card:hover {
    border-color: var(--kscw-blue, #4A55A2);
    color: var(--accent-text, #4A55A2);
  }
  /* Toggle switches */
  .toggle-group {
    display: flex;
    gap: var(--space-lg);
    flex-wrap: wrap;
    align-items: center;
    margin-top: var(--space-xs);
  }
  .toggle-label {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    cursor: pointer;
    font-weight: normal;
  }
  .toggle-switch {
    position: relative;
    display: inline-flex;
    width: 44px;
    height: 24px;
    flex-shrink: 0;
    vertical-align: middle;
  }
  .toggle-switch input {
    opacity: 0;
    width: 0;
    height: 0;
    position: absolute;
  }
  .toggle-slider {
    position: absolute;
    inset: 0;
    background: var(--border);
    border-radius: 12px;
    transition: background 0.2s ease;
  }
  .toggle-slider::before {
    content: '';
    position: absolute;
    width: 18px;
    height: 18px;
    left: 3px;
    bottom: 3px;
    background: white;
    border-radius: 50%;
    transition: transform 0.2s ease;
  }
  .toggle-switch input:checked + .toggle-slider {
    background: var(--kscw-blue, #4A55A2);
  }
  .toggle-switch input:checked + .toggle-slider::before {
    transform: translateX(20px);
  }
  /* Form feedback states */
  #already-member-warn {
    margin-top: var(--space-sm);
    padding: var(--space-md);
    border-radius: var(--radius-md, 8px);
    border: 1px solid #ef4444;
    background: rgba(239, 68, 68, 0.08);
    color: var(--text-primary);
    font-size: 0.9rem;
    line-height: 1.5;
  }
  #already-member-warn strong { display: block; margin-bottom: 0.25rem; }
  #already-member-warn a { color: var(--kscw-gold, #b8860b); }

  #form-feedback {
    padding: 0.875rem 1rem;
    border-radius: var(--radius-md, 8px);
    font-size: var(--text-sm);
    font-weight: 500;
    margin-top: var(--space-md);
  }
  #form-feedback.success {
    background: rgba(34, 197, 94, 0.1);
    color: #16a34a;
    border: 1px solid rgba(34, 197, 94, 0.3);
  }
  #form-feedback.error {
    background: rgba(239, 68, 68, 0.1);
    color: #dc2626;
    border: 1px solid rgba(239, 68, 68, 0.3);
  }
  @media (prefers-color-scheme: dark) {
    #form-feedback.success { color: #4ade80; }
    #form-feedback.error { color: #f87171; }
  }
  #turnstile-container {
    min-height: 0;
  }
  /* Success modal — created at runtime by registration-form.js. These MUST be
     :global; scoped selectors carry a build-time data-astro-cid attribute that
     JS-injected elements never get, so the modal would render unstyled (no
     overlay/centering) at the bottom of the page and look like "nothing". */
  :global(.success-modal-overlay) {
    position: fixed; inset: 0; z-index: 9999;
    background: rgba(0,0,0,0.5); backdrop-filter: blur(4px);
    display: flex; align-items: center; justify-content: center;
    opacity: 0; transition: opacity 0.2s ease;
  }
  :global(.success-modal-overlay.visible) { opacity: 1; }
  :global(.success-modal) {
    background: var(--card-bg, #1e293b); border: 1px solid rgba(34,197,94,0.3);
    border-radius: var(--radius-lg, 12px); padding: 2rem 2.5rem;
    text-align: center; max-width: 420px; width: 90%;
    box-shadow: 0 0 40px rgba(34,197,94,0.15);
    transform: scale(0.95); transition: transform 0.2s ease;
  }
  :global(.success-modal-overlay.visible .success-modal) { transform: scale(1); }
  :global(.success-modal-icon) { margin-bottom: 1rem; }
  :global(.success-modal-msg) { font-size: var(--text-base); color: var(--text-primary, #e2e8f0); line-height: 1.6; margin: 0 0 1.5rem; }
  :global(.success-modal-btn) {
    background: #22c55e; color: #fff; border: none; border-radius: var(--radius-md, 8px);
    padding: 0.625rem 2rem; font-size: var(--text-sm); font-weight: 600;
    cursor: pointer; transition: background 0.15s;
  }
  :global(.success-modal-btn:hover) { background: #16a34a; }
  small {
    display: block;
    font-size: var(--text-xs, 0.75rem);
    margin-top: var(--space-xs);
  }
  /* Searchable nationality dropdown */
  .nationality-wrapper {
    position: relative;
  }
  .nationality-trigger {
    display: flex;
    align-items: center;
    justify-content: space-between;
    cursor: pointer;
    text-align: left;
    appearance: none;
    font-family: inherit;
    font-size: var(--text-base);
    color: var(--text-primary);
  }
  .nationality-trigger svg {
    flex-shrink: 0;
    opacity: 0.5;
    transition: transform 0.2s ease;
  }
  .nationality-wrapper.open .nationality-trigger svg {
    transform: rotate(180deg);
  }
  .nationality-dropdown {
    display: none;
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    z-index: 50;
    background: var(--bg-white);
    border: 1px solid var(--border);
    border-radius: var(--radius-md, 8px);
    margin-top: 4px;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
    overflow: hidden;
  }
  .nationality-wrapper.open .nationality-dropdown {
    display: block;
  }
  .nationality-search {
    border: none !important;
    border-bottom: 1px solid var(--border) !important;
    border-radius: 0 !important;
    box-shadow: none !important;
  }
  .nationality-search:focus {
    box-shadow: none !important;
  }
  .nationality-options {
    max-height: 240px;
    overflow-y: auto;
  }
  :global(.nationality-opt) {
    padding: 0.875rem 1.125rem;
    cursor: pointer;
    font-size: var(--text-sm);
    color: var(--text-primary);
    transition: background 0.1s ease;
  }
  :global(.nationality-opt:hover),
  :global(.nationality-opt.highlighted) {
    background: rgba(74, 85, 162, 0.08);
  }
  :global(.nationality-opt.selected) {
    color: var(--accent-text, #4A55A2);
    font-weight: 600;
  }
  :global(.nationality-divider) {
    border: none;
    border-top: 1px solid var(--border);
    margin: 0;
  }
  /* Selected-nationality chips (multi-select). Rendered above the trigger so the
     picked passports stay visible while the list is open. */
  .country-chips:not(:empty) {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-xs);
    margin-bottom: var(--space-xs);
  }
  :global(.country-chip) {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 4px 10px;
    border-radius: 999px;
    background: var(--surface-alt, rgba(0, 0, 0, 0.05));
    border: 1px solid var(--border);
    font-size: 0.85rem;
    line-height: 1.3;
  }
  :global(.country-chip button) {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    /* ≥24px hit area inside a compact chip; the chip row itself sits above a
       44px trigger so the control group stays thumb-friendly. */
    width: 24px;
    height: 24px;
    margin: -4px -6px -4px 0;
    padding: 0;
    border: none;
    background: none;
    color: var(--text-secondary);
    font-size: 1.1rem;
    line-height: 1;
    cursor: pointer;
  }
  :global(.country-chip button:hover) {
    color: var(--text);
  }
  /* Team multiselect dropdown */
  .team-wrapper {
    position: relative;
  }
  .team-trigger {
    display: flex;
    align-items: center;
    justify-content: space-between;
    cursor: pointer;
    text-align: left;
    appearance: none;
    font-family: inherit;
    font-size: var(--text-base);
    color: var(--text-primary);
  }
  .team-trigger svg {
    flex-shrink: 0;
    opacity: 0.5;
    transition: transform 0.2s ease;
  }
  .team-wrapper.open .team-trigger svg {
    transform: rotate(180deg);
  }
  .team-dropdown {
    display: none;
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    z-index: 50;
    background: var(--bg-white);
    border: 1px solid var(--border);
    border-radius: var(--radius-md, 8px);
    margin-top: 4px;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
    overflow: hidden;
  }
  .team-wrapper.open .team-dropdown {
    display: block;
  }
  .team-options {
    max-height: 280px;
    overflow-y: auto;
  }
  :global(.team-opt) {
    padding: 0.875rem 1.125rem;
    cursor: pointer;
    font-size: var(--text-sm);
    color: var(--text-primary);
    transition: background 0.1s ease;
    display: flex;
    align-items: center;
    gap: var(--space-sm);
  }
  :global(.team-opt:hover) {
    background: rgba(74, 85, 162, 0.08);
  }
  :global(.team-opt.selected) {
    color: var(--accent-text, #4A55A2);
    font-weight: 600;
  }
  /* BB docs download list */
  .bb-docs-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
    margin-top: var(--space-xs);
  }
  .bb-doc-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-md);
    padding: 0.5rem 0.75rem;
    border: 1px solid var(--border);
    border-radius: var(--radius-md, 8px);
    font-size: var(--text-sm);
  }
  .bb-doc-item span {
    font-weight: normal;
    color: var(--text-secondary);
  }
  .bb-doc-download {
    font-size: var(--text-xs, 0.75rem);
    font-weight: 600;
    color: var(--accent-text, #4A55A2);
    text-decoration: none;
    white-space: nowrap;
  }
  .bb-doc-download:hover {
    text-decoration: underline;
  }
  /* Fee summary — what the invoice will say, filled by registration-form.js */
  .fee-summary {
    margin-bottom: var(--space-md);
    padding: var(--space-md);
    border: 1px solid var(--border);
    border-radius: var(--radius-md, 8px);
    background: var(--bg-light);
  }
  .fee-summary-title {
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: var(--space-sm);
  }
  .fee-table {
    width: 100%;
    border-collapse: collapse;
    font-size: var(--text-sm);
  }
  .fee-table th,
  .fee-table td {
    padding: 0.375rem 0;
    vertical-align: top;
    border-bottom: 1px solid var(--border);
  }
  /* global.css styles every <th> as a column header — display font, uppercase,
     letter-spaced and nowrap. These are row labels: body font, sentence case,
     and they must WRAP, or the surcharge hint pushes the amounts off-screen. */
  .fee-table th {
    font-family: inherit;
    font-size: var(--text-sm);
    text-transform: none;
    letter-spacing: normal;
    white-space: normal;
    text-align: left;
    font-weight: normal;
    color: var(--text-secondary);
    padding-right: var(--space-md);
  }
  .fee-table th small {
    font-size: var(--text-xs);
    color: var(--text-secondary);
  }
  .fee-table td {
    text-align: right;
    white-space: nowrap;
    font-variant-numeric: tabular-nums;
    color: var(--text-primary);
  }
  .fee-table tr:hover td {
    background: transparent;
  }
  .fee-table tfoot th,
  .fee-table tfoot td {
    border-bottom: none;
    border-top: 2px solid var(--border);
    padding-top: var(--space-sm);
    font-weight: 700;
    color: var(--accent-text, #4A55A2);
  }
  .fee-summary p {
    margin: var(--space-sm) 0 0;
    font-size: var(--text-sm);
    color: var(--text-primary);
    line-height: 1.5;
  }
  .fee-summary .fee-summary-note {
    font-size: var(--text-xs, 0.75rem);
    color: var(--text-secondary);
  }
</style>
